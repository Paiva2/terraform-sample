version: "3"

services:
  api:
    container_name: app-java
    image: paiva2/app-java:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
